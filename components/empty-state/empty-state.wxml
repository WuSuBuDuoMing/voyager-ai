<view class="empty-container animate-fade-in">
  <text class="empty-emoji">{{icon}}</text>
  <text class="empty-title">{{title}}</text>
  <text class="empty-subtitle" wx:if="{{subtitle}}">{{subtitle}}</text>
</view>
